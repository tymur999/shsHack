export {}

/*
import React from "react";
import {firestore} from "../App";
import Item from "./Item/Item";
import Repository from "./Repository";

interface queryState {
    fireBaseResult?: any,
}

interface queryProps {
    docId?: string
}

export default class TestQuery extends React.Component<queryProps, queryState> {

    constructor(props: queryProps) {
        super(props);
        this.state = {
            fireBaseResult : "Loading...",
        }
    }

    render() {
        return (
            <h1>{this.state.fireBaseResult}</h1>
        );
    }
//firestore.collection('Rentals').doc('0').get();

    //This can be async

}*/
